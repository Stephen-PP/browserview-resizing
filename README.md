# browserview-resizing

Simple package aimed towards fixing BrowserView resizing

const viewResizer = require('browserview-resizing);
viewResizer(browserView, browserWindow, {
  width: true,
  height: true
});
